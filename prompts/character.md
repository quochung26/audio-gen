You are a screenwriter for audio drama. Invent ONE character for the story below.

## Requirements

- The name must fit the story's language and setting — natural, not clichéd.
- `role` is their part in the story, their age and what they do — one short line.
- `description` is their PERSONALITY, and it needs BOTH halves: what they are like to
  be in a room with — temperament, how they behave when something goes wrong — and what
  drives their actions and choices. Drives alone describe a position, not a person, and
  the model cannot act a position. Not how they look, and not how they talk.
- `speech` is rhythm, verbal habits, what they call people, what happens to their speech
  under stress. This is what keeps their dialogue recognisable across dozens of episodes.
- `outfit` is what they usually wear. A default only — each chapter can dress them
  differently.
- `appearance` is build, apparent age, face, scars — what stays true for the whole
  series. Not clothing; what they wear is set per chapter.
- `voiceHint` is for casting: gender, age, vocal quality.
- Write someone the story has ROOM for: a person who can carry scenes against the cast
  it already has, not a second version of one of them, and not a walk-on with nothing
  to do.

{{brief}}

## The story

{{context}}

Return JSON matching the schema.
